# iSdu：项目执行规则

本文件对仓库内所有开发、评审和自动化任务生效。若任务与本文件冲突，先停止并向用户说明，不得自行扩大范围。

## 1. 产品定位

比赛版是“单用户、单校区、无社交功能的校园地图个人日记微信小程序”。它用时间和校园空间共同组织个人记忆。

核心闭环：

1. 打开二维校园插画地图。
2. 在地图上手动选择位置。
3. 添加 1 至 3 张照片、正文、地点名称和心情。
4. 保存日记并在原位置显示记忆标记。
5. 点击标记查看、编辑或删除日记。
6. 从时间轴按月份回顾，并能在时间轴与地图位置间互相跳转。
7. 查看个人记忆概览。

距离比赛提交时间短，优先级依次为：真机可运行、闭环完整、地图体验、统一视觉、五分钟可演示、长期扩展。

## 2. 比赛版范围

### P0：必须完成

- 校园地图主页：拖动、缩放、记忆标记、标记详情、视图复位、空状态、失败状态。
- 新建日记：地图选点、1 至 3 张图片、正文、地点、心情、自动时间、保存。
- 日记详情：完整展示、编辑、删除二次确认、返回地图位置。
- 时间轴：按月分组、缩略图、日期、地点、摘要、详情跳转、地图跳转、空/加载/图片失败状态。
- 个人记忆概览：总数、记录天数、地点数、最常记录地点、最近记录。
- 本地 Demo 持久化；正式模式使用 CloudBase 文档数据库与云存储。

### P1：只有 P0 全部通过后才能开始

- 按月份筛选地图。
- 简化时光回放。
- 首次记录地点的点亮效果。
- 地点记忆卡片或印章。
- 个人统计卡片。
- 地图标记聚合。
- 首次使用引导。

P1 中优先级最高的是“时光回放”，但它不能挤占 P0 的测试与真机修复时间。

## 3. 禁止进入比赛版的长期功能

不得实现或迁移：评论、点赞、好友、关注、私信、公开动态、校园朋友圈、浏览其他用户内容、实时定位、GPS 轨迹、附近的人、多校区、统一身份认证、视频日记、推荐算法、AI 自动生成日记、管理员后台、复杂分享权限、社交信息流。

不得因第三方项目已经包含这些功能而把它们带入主项目。

## 4. 固定技术路线

- 平台：微信原生小程序。
- 语言：TypeScript。
- 视图与样式：WXML、WXSS。
- 渲染器：比赛版使用 WebView；P0 期间不切换 Skyline。
- UI：`tdesign-miniprogram` 是唯一正式 UI 组件库，按页面只注册实际使用的组件。
- 云端：微信云开发 CloudBase 文档数据库和云存储；不搭建独立服务器。
- 本地模式：通过统一 Repository 接口切换本地 Demo 与 CloudBase 实现。
- 测试与质量：TypeScript 类型检查、ESLint、Prettier；纯逻辑单元测试和关键流程真机检查。
- 版本管理：Git。

禁止引入 Godot、React、Vue、uni-app、大型无关框架或第二套正式 UI 组件库。偏离路线前必须提交书面理由、收益、代价和迁移风险，并等待批准。

## 5. 页面与代码结构

计划页面：

```text
miniprogram/
  pages/
    map/index            # 校园地图主页
    editor/index         # 新建/编辑日记
    detail/index         # 日记详情
    timeline/index       # 时间轴
    profile/index        # 个人记忆概览
  components/            # 项目自研业务组件
  services/
    repositories/        # Repository 接口、本地与 CloudBase 实现
  models/                # 日记、坐标、筛选条件等类型
  utils/                 # 无 UI 的纯函数
  assets/                # 原创或明确授权的静态资源
```

地图、时间轴和概览可以进入 TabBar；编辑页和详情页使用普通页面导航。页面之间通过日记 ID 和比例坐标跳转，不传递整份大对象。

## 6. 地图实现不变量

- 比赛主页不是腾讯地理导航地图，不使用原生 `<map>` 作为核心底座。
- 使用 `movable-area` / `movable-view` 承载原创或占位二维地图、缩放、拖动和标记。
- 每个位置保存相对原始地图的 `xRatio`、`yRatio`，取值范围必须是 `[0, 1]`。
- 标记必须放在与地图图片相同的变换容器中；禁止按固定屏幕像素保存位置。
- 屏幕坐标与比例坐标转换必须写成可单元测试的纯函数，并覆盖不同视口、平移和缩放场景。
- 不申请定位权限，不使用 GPS。

## 7. 数据与隐私安全

- 日记默认私有，只能由创建者本人读取、修改和删除。
- 不得提供公开读取全部日记的接口、查询或云函数。
- CloudBase 集合使用“仅创建者可读写”或等价的自定义安全规则；任何公共读取配置都视为阻断缺陷。
- 客户端查询也必须按当前用户作用域设计，不能把安全完全寄托在 UI 隐藏上。
- 图片按用户隔离路径存储；数据库保存 `fileID`，不得把会过期的临时 URL 当永久数据。
- 密钥、真实 AppID、环境 ID、OpenID、私有配置和访问令牌不得提交。
- `project.private.config.json`、环境文件和本机 CloudBase 配置必须保持忽略。
- 日志不得输出完整日记正文、OpenID、文件临时签名 URL 或其他敏感数据。

## 8. 第三方与原创性

- 正式使用的第三方内容必须登记到 `docs/THIRD_PARTY_NOTICES.md`。
- 允许使用许可证清晰的组件库、官方示例和少量通用代码；必须保留许可证、版权和来源。
- 禁止整仓换皮、删除署名、隐藏来源、复制许可证不明或要求先联系作者的代码。
- 第三方完整仓库只能放在主仓库之外的临时评估目录，不得提交。
- 校园插画地图、比例坐标系统、空间日记交互和时光回放属于本项目原创核心，必须自研。

## 9. Git 规则

- 开始前检查当前分支、`git status` 和未提交更改。
- 保留用户已有文件和更改；不得覆盖、丢弃或重写用户工作。
- 禁止 `git reset --hard`、`git clean -fd`、强制推送和覆盖历史。
- 每次提交只包含一个清晰里程碑；不要混入临时克隆、密钥、真实 AppID、私有配置或大体积无关文件。
- 提交前检查 `git diff`、`git status`、新增文件体积和敏感配置。
- 未得到明确要求时，不推送远端、不创建 PR、不部署。

## 10. 里程碑与停止条件

- 每轮只执行用户指定的一个里程碑。
- 先写清验收标准，再实施，再做与风险相称的验证。
- 不得擅自加入“顺手可做”的需求或长期功能。
- 当前里程碑完成后必须停止，汇报已完成、未验证、风险和人工步骤，等待用户验收。
- 未通过验收不得自动进入下一里程碑。
