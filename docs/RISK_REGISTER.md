# 风险登记册

更新日期：2026-08-11

等级说明：

- 严重程度：低 / 中 / 高 / 致命。
- 发生概率：低 / 中 / 高。
- “致命”表示可能造成隐私泄露、比赛无法提交、核心闭环不可演示或原创性/许可不合格。

| ID   | 风险                                         | 严重程度 | 概率               | 预防方案                                                                                   | 发生后的处理方案                                                               |
| ---- | -------------------------------------------- | -------- | ------------------ | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------ |
| R-01 | 第三方项目过时                               | 高       | 高                 | 不以旧日记项目为底座；核实最近提交、旧 API 和工具配置；核心自行实现                        | 停止迁移，隔离受影响代码；换成微信官方当前 API 并补回归测试                    |
| R-02 | 微信 API / 基础库兼容问题                    | 高       | 中                 | 以官方 Demo/文档为准；固定基础库与 WebView 渲染；每个复杂组件真机验证                      | 降低到已验证 API 组合；替换单个组件，不整体换框架；记录兼容矩阵                |
| R-03 | 小程序包体超限或首包过大                     | 高       | 中                 | TDesign 按页面注册；不混用 UI；每次集成记录构建包体；压缩图片和地图                        | 删除未使用组件/资源；延后非 P0；必要时在 P0 稳定后再做分包                     |
| R-04 | 地图缩放后标记漂移                           | 致命     | 中（待真机后复评） | 地图和标记同一变换容器；持久化比例坐标；纯函数测试多尺寸与往返换算                         | 暂停业务叠加，回到地图技术样片；修正坐标模型并迁移已有本地数据                 |
| R-05 | 图片选择、持久化或后续上传失败               | 高       | 高                 | 限制 1 至 3 张；临时图全部复制到受管用户目录后才写 Repository；云阶段再设计重试与 `fileID` | 新图任一保存失败则回滚本轮文件且不写日记；旧图清理失败明确提示，不删除有效正文 |
| R-06 | CloudBase 权限配置错误                       | 致命     | 中                 | 集合和存储仅创建者可读写；环境 ID 显式；双用户越权反向测试作为发布门禁                     | 立即停止演示/发布并收紧规则；审计日志和受影响数据；轮换暴露链接/配置           |
| R-07 | 私人日记或照片泄露                           | 致命     | 中                 | 不做公开查询；日志脱敏；存储私有；不保存长期公开 URL；最小权限                             | 下线相关入口；撤销公共权限和链接；删除缓存；通知用户并复盘数据范围             |
| R-08 | 第三方许可证不清或元数据冲突                 | 致命     | 中                 | 无明确 LICENSE 不复制；逐文件记录来源；第三方声明随引入更新                                | 删除无法证明许可的内容并重写；联系作者或更换方案；更新比赛披露                 |
| R-09 | 复制过多无关代码                             | 高       | 中                 | 从空骨架自建；每个新增文件映射到 P0；禁止整仓迁移；评审超范围关键词                        | 回退独立迁移提交；提取真正需要的最小接口并原创重写                             |
| R-10 | 25 天工期不足                                | 致命     | 高                 | 严格 P0 → P1；一个里程碑一轮；每天保持可演示版本；本地 Demo 保底                           | 冻结 P1、动效和非关键美化；优先真机闭环、Demo 脚本和修复                       |
| R-11 | 视觉风格不统一                               | 中       | 中                 | 单一 TDesign；设计 token；统一照片卡片/状态页/标记；不复制商城主题                         | 做集中视觉审查；先统一色彩、字体、间距、圆角，再修个别页面                     |
| R-12 | TDesign Skyline 兼容不完整                   | 高       | 高（若启用）       | 比赛版固定 WebView；不启用 Skyline；复杂组件逐个真机验证                                   | 回退 WebView；禁止为 Skyline 大规模改造 P0                                     |
| R-13 | TDesign 自身出现阻断缺陷                     | 高       | 低至中             | 精确锁版本；下一里程碑只做最小组件冒烟；保留原生组件降级路径                               | 优先用原生组件替代单个阻断组件；只有系统性阻断才重新评估 Vant                  |
| R-14 | TypeScript / 开发者工具编译链不一致          | 高       | 中                 | 最小骨架先验证；命令行类型检查 + 开发者工具编译；配置保持单一                              | 修复配置并固定工具版本；不能用 `any` 或整仓改 JS 掩盖问题                      |
| R-15 | 本地 Demo 数据损坏或版本升级丢失             | 高       | 中                 | 版本化 key、schema 校验、原子快照、显式 Seed/重置、迁移测试                                | 进入可恢复空/错误状态；保留原值备份；提供一次性迁移/重置选项                   |
| R-16 | 临时图片路径重启后失效                       | 高       | 高                 | 本地模式把选择结果持久化为小程序本地文件；Seed 用仓库内合规资产                            | 标记图片失败并允许重选；不删除正文/坐标；修复路径后补迁移                      |
| R-17 | 云端与本地数据实现行为不一致                 | 高       | 中                 | 统一 Repository 契约和契约测试；页面不直接调用存储 API                                     | 以契约测试定位差异；切回本地 Demo 保证比赛演示；修复云实现                     |
| R-18 | 真实 AppID、环境 ID、OpenID 或令牌误提交     | 致命     | 中                 | 私有配置忽略；提交前敏感扫描；示例只用占位符；不在日志输出                                 | 立即撤销/轮换凭据；从待提交内容移除；必要时按安全流程清理历史                  |
| R-19 | CloudBase Skills/MCP 引入过大权限            | 高       | 中                 | 当前不安装；后续固定版本、项目级安装、MCP 单独审批、显式环境 ID                            | 禁用/卸载连接；撤销令牌和权限；检查是否产生资源变更                            |
| R-20 | 模拟器正常但真机失败                         | 致命     | 中                 | 每个关键流程真机检查；至少一台比赛演示机；记录微信/系统版本                                | 冻结新功能，优先修演示机；准备稳定本地 Demo 数据和离线演示路径                 |
| R-21 | 正式校园地图素材版权或体积不合格             | 高       | 中至高             | 使用原创或有明确授权的地图；保留提示词、源文件、生成记录和授权证据；导出前做尺寸/压缩预算  | 替换来源明确的简化版；删除无法证明使用权的素材；按冻结宽高比重新导出并迁移坐标 |
| R-22 | Demo 视频在五分钟内无法讲清闭环              | 高       | 中                 | 尽早写演示脚本；Seed 数据覆盖地图、详情、时间轴、回放；每周彩排                            | 删除非核心镜头；固定 3 至 4 条代表记忆；突出“空间 + 时间”差异                  |
| R-23 | 本地 Storage 或用户文件目录空间不足          | 高       | 中                 | 只保存结构化文字和路径；不存 Base64；限制 3 图；请求压缩并限制单图 5 MiB；后续补总量预算   | 阻止无效日记落库并提示清理空间；保留旧数据；提供安全的孤儿文件清理策略         |
| R-24 | Repository 与图片文件跨步骤部分失败          | 致命     | 中                 | 新建先存图后写数据且失败回滚；编辑成功写数据后才删旧图；删除先删数据再清理无引用图         | 明确报告未清理路径；绝不因清理失败让剩余日记引用失效；后续增加孤儿清理任务     |
| R-25 | 星点数量增加后重叠、遮挡或触控困难           | 高       | 高                 | 当前先保证比例绑定；下一阶段按视口坐标和缩放等级设计聚合，不改原始比例                     | 限制同屏渲染或加入分桶聚合；选中后展开；不通过改坐标“错开”真实地点             |
| R-26 | 当前地图留白裁切或宽高比变化导致历史坐标错位 | 致命     | 中                 | 冻结 `1448 × 1086` 整图坐标参考和地图版本；旧版本记录迁移前不投影到新地图                  | 暂停换图发布；按旧图到新图的仿射关系迁移比例并保留备份；真机抽样校准           |

## 当前最高优先级

1. R-10：25 天工期。
2. R-04 / R-26：地图缩放与换图后的坐标正确性。
3. R-20：完整本地闭环的真机可运行性。
4. R-21：当前及正式地图的来源与原创证据。
5. R-23 / R-24：本地图片空间和跨步骤失败。
6. R-06 / R-07：未来 CloudBase 权限和隐私。

每个里程碑结束时更新本表的概率、状态和新证据；风险未关闭不等于里程碑一定阻断，但“致命”风险必须有明确发布门禁。

## 骨架里程碑证据

- R-12：`app.json` 明确使用 WebView，公共工程配置关闭 Skyline；风险预防已落地。
- R-13：TDesign `1.15.3` 已安装，Button / Empty 已由用户确认通过开发者工具人工验收；独立真机记录仍未提供。
- R-14：TypeScript `5.9.3` 命令行类型检查已通过，开发者工具编译已由用户人工验收；地图里程碑仍需重新编译验证。
- R-18：公共工程只使用 `touristappid`，真实 AppID 和 `project.private.config.json` 保持忽略。
- R-21：用户已提供目标校园图作为后续参考，但图片未复制进仓库；来源、授权、原始分辨率与比赛使用权仍需在地图里程碑前确认。

## 地图与本地闭环证据

- R-03：当前地图为 `1448 × 1086` JPEG，490,518 字节；未使用 Base64，未新增地图运行时依赖。替换后的开发者工具包体与编译耗时仍待人工记录。
- R-04：地图、照片圆点和星点位于同一个 `movable-view`；坐标只保存为比例。纯函数已覆盖以 `movable-view` 中心为缩放原点的正逆变换，修正了旧版左上角公式在 `2×` 下的漂移风险。模拟器和真机仍是发布门禁。
- R-05 / R-24：本地图片服务只写入 `USER_DATA_PATH/sdu-memory/images/<memoryId>/`，部分保存失败会回滚；编辑和删除只在 Repository 状态安全后清理无引用文件。页面会报告清理失败，自动测试不能替代真机文件系统验证。
- R-15：Storage 使用 `sdu-memory:memories:v1` 与 `schemaVersion: 1`；损坏 JSON 或未知 schema 会进入显式错误状态且不覆盖原值。Demo 只在 key 从未存在时 Seed，用户删到空数组后不会自动重灌。
- R-16：`wx.chooseMedia` 临时路径不会写入 Memory；只有 `saveFile` 成功后的受管路径才进入 Repository。图片文件被系统或用户清理后，页面降级并要求移除或重选。
- R-20：完整新建、重启读取、编辑、删除、相册/相机权限、前后台切换和真机文件路径仍需用户在开发者工具与至少一台真机验证；当前不得标记通过。
- R-21 / R-26：用户提供的当前地图已进入仓库，但来源证据、源文件和比赛使用权仍需归档；图片包含较大留白，正式裁切前必须先制定比例坐标迁移。
- R-23：当前已请求压缩、限制单篇最多 3 图和单图 5 MiB，但尚未实现全库总容量统计、二次压缩和孤儿文件巡检，风险保持开放。
- R-25：当前没有星点聚合，属于明确的下一阶段能力，不阻断少量日记闭环验收。

## 本地数据公测版发布证据

- R-03：`project.config.json` 已明确排除 Demo Seed、纯逻辑测试和目录说明文件；最终主包大小仍必须以微信开发者工具上传面板为准。
- R-07：应用内已说明数据仅保存于当前设备，添加照片前再次说明用途；“时光 → 数据管理”提供二次确认的一键清除能力。公众平台隐私保护指引和运营者联系方式仍是提审门禁。
- R-15：正式运行模式为 `local`，不再调用 Demo Seed；新用户首次打开是空状态，一键清除后保留合法空快照。
- R-18：敏感扫描只发现公共占位 `touristappid` 与文档说明；地图源 PNG 已通过 `.gitignore` 排除。正式 AppID 继续只放在本机私有配置。
- R-20：发布候选必须增加一台 iOS 和一台 Android 的完整闭环与一键清除测试；在人工记录完成前不得标记关闭。
- R-21：运行时地图已经优化到约 479 KiB，但源图生成过程、素材权利和“非官方”品牌边界仍需运营者归档确认。
