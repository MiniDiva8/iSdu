# 项目状态

更新日期：2026-07-31

## 当前阶段

最小微信原生 TypeScript 小程序骨架里程碑已完成，并于 2026-07-31 通过用户人工验收。下一里程碑为“地图技术原型”，本轮不启动该里程碑。

## 当前仓库状态

- 当前分支：`feat/miniprogram-skeleton`。
- 基线为已验收的 `research/open-source-evaluation` 分支提交。
- 本里程碑代码与文档在当前分支以一个原子提交归档。
- 尚无远端。
- 项目仓库暂时保持私有。
- 当前不添加 MIT 或其他项目自身的开源许可证。
- 已建立微信原生 TypeScript 小程序目录和公共 `project.config.json`。
- 公共工程 AppID 为安全占位值 `touristappid`；没有真实 AppID。
- 已生成 `package.json` 和 `package-lock.json`；`node_modules` 保持忽略。
- 当前工作区没有保留 `miniprogram_npm`；该目录保持忽略，不进入 Git。
- 尚无 CloudBase 环境、数据库集合、云函数或云存储配置。

## 已完成事项

- 创建地图、记录、时光三个 Tab 占位页。
- 创建可从地图页进入的详情页和个人页占位页。
- 使用 WXML / WXSS 建立温暖纸色、校园绿、圆角与安全区等最小设计变量。
- 明确使用 WebView，不启用 Skyline。
- 建立 `config/runtime.ts` 和最小 Demo Repository 健康状态接口；没有 CRUD。
- 精确安装 `tdesign-miniprogram@1.15.3`，只注册 Button 和 Empty。
- 精确安装 TypeScript、微信 API 类型、ESLint、typescript-eslint 和 Prettier。
- 命令行 TypeScript 类型检查和 ESLint 已通过。
- 新增开发者工具、构建 npm、预览和真机测试手册。
- 用户已确认微信开发者工具人工验证通过，最小骨架能够正常导入、编译和预览。
- 用户已按本里程碑验收范围确认三个 Tab、详情/个人页跳转和少量 TDesign 组件可用。

## 人工验收结果

- 微信开发者工具：通过。结果来自用户于 2026-07-31 的人工验收确认。
- 真机：本次验收反馈没有提供独立的真机测试结论、手机型号、系统版本、微信版本或控制台记录，因此实际状态记为“详细结果未提供”，不补写未经确认的通过结论。
- 自动化边界：当前环境没有可调用的微信开发者工具 CLI；以上人工结果不能由命令行复现，但不影响本里程碑按用户验收结果归档。

## 未完成事项

- 尚未留档 `miniprogram_npm` 实际生成大小、开发者工具主包/总包数据或编译耗时。
- 尚未留档独立的真机型号、系统/微信版本、控制台结果和异常记录。
- 尚未建立带存储或 CRUD 的本地 Repository。
- 尚未制作地图技术样片或正式校园地图。
- 尚未实现任何 P0 业务功能。
- 尚未接入或配置 CloudBase。
- 尚未准备 Demo 视频脚本和比赛 PPT。

## 阻断问题

当前没有阻断本里程碑提交的问题。微信开发者工具人工验收已由用户确认通过；真实 AppID 仍不得进入仓库。

以下条件会阻断后续对应里程碑：

- 真机预览需要用户提供可用的小程序 AppID，并只在私有配置中设置。
- CloudBase 接入需要用户开通并确认独立环境、资源操作授权和计费/配额状态。
- 正式地图需要原创设计或可证明授权的素材。

## 未能验证

- `miniprogram_npm`、主包/总包的精确大小及编译耗时。
- TDesign 组件、三个 Tab、详情/个人页和安全区在具体目标真机上的表现。
- 真机控制台是否没有未处理异常；当前没有可核对的真机记录。
- 用户提供的目标校园图是否具有比赛使用授权和足够的原始分辨率；图片当前未进入仓库。

## TDesign 包体与编译观察

- npm 安装目录中的 `tdesign-miniprogram@1.15.3` 共 1,162 个文件、1,589,427 字节；这是 npm 解压体积，不是小程序主包体积。
- Button 和 Empty 被主包页面引用，因此 TDesign 会增加主包内容和首次 npm 构建/编译工作量。
- 页面 JSON 只注册实际使用的两个组件，没有全局注册或第二套 UI 库。
- 微信开发者工具人工验证已通过，但没有留档 `miniprogram_npm`、主包/总包或编译耗时，因此仍不能量化 TDesign 的实际增量。

## 下一里程碑

下一里程碑为“地图技术原型”：使用合规占位图验证 `movable-area` / `movable-view`、边界约束和比例坐标。本轮只归档小程序骨架，不自动开始地图开发。

## 需要用户人工完成

- 进入后续地图技术原型前，确认所用地图素材具备比赛使用授权。
- 后续真机验收时，记录比赛演示手机型号、系统版本、微信版本、包体和控制台结果；私人配置继续保持忽略。
