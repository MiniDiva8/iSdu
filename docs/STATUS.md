# 项目状态

更新日期：2026-08-15

正式产品名称：`iSdu`。代码中的 `sdu-memory:*` Storage 键和本地图片目录继续作为兼容性内部标识保留，避免改名导致既有本地日记和图片无法读取。

## 当前阶段

比赛第一版已经冻结在 `main` 分支提交 `4f517e9 feat: finalize competition v1`。该版本完成本地地图日记闭环，继续作为可独立运行、可回退的稳定基线。

当前进入第二阶段第一个里程碑：“社交 V2 规则与技术设计”。本里程碑只完成范围、权限、数据结构、接口边界、迁移策略和测试门禁，不创建 CloudBase 环境、不部署云函数、不修改现有本地日记业务。

第二阶段已经确认的产品范围只有：

- 通过微信小程序云环境取得可信用户身份；
- 通过邀请建立 iSdu 内部双向好友关系，不读取微信通讯录或微信好友列表；
- 回忆可设为“仅自己”“指定好友”“全部好友”；
- 好友可对有权查看的回忆点赞；
- 地图单独显示过去 24 小时内、当前用户有权查看的好友回忆点。

全网公开、陌生人浏览、评论、私信、群组、好友推荐、通讯录匹配和 GPS 仍不在当前范围内。完整设计见 `docs/SOCIAL_V2_DESIGN.md`。

## 当前仓库状态

- 比赛第一版基线：`main` / `4f517e9 feat: finalize competition v1`。
- 当前设计分支：`codex/social-v2`，从上述稳定基线创建。
- 当前分支尚未提交或推送；本轮变更仅限第二阶段规则与设计文档。
- 尚未创建 CloudBase 集合、存储桶、索引、云函数或环境配置，也没有迁移任何本地数据。
- 公共工程配置继续使用安全占位 AppID；真实 AppID、环境 ID、OpenID、密钥和私人配置不得进入 Git。
- `node_modules`、构建生成的 `miniprogram_npm` 和 `project.private.config.json` 均不应提交。

## 已完成：地图与选点

- 使用原生 `movable-area` / `movable-view` 显示、拖动和缩放单张校园插画。
- 缩放范围为 `1×`–`2.5×`，cover 布局保证最小比例仍覆盖视口。
- 当前地图资源已替换为用户提供的清理版校园插画：`1448 × 1086` JPEG，490,518 字节（约 479 KiB）。
- 当前地图与坐标版本为 `campus-center-clean-illustration-2026-08-v3-rect`；旧地图版本及修复前的 v2 错误坐标继续保留，但不投影到新地图，防止标记落在错误建筑。
- 地图中心准星选点已实现；选点时关闭惯性，确认时有防重复导航锁。
- 选点确认时读取地图视口与图片的真实屏幕矩形，直接按准星相对图片的位置计算比例，不再依赖真机可能存在差异的 `movable-view x/y/scale` 理论变换。
- 比例坐标保存为 `mapXRatio` / `mapYRatio`，范围 `[0,1]`，路由传递保留 6 位小数。
- 地图在 `onShow` 从 Repository 重新读取，保存、编辑和删除后会刷新星点。
- 默认显示心情色小点；用户可切换为照片标记，无照片或图片丢失时自动回退到心情色点。
- 点击标记显示简短卡片并可进入详情。
- 加载、失败、重试、空数据、损坏 Repository、非法坐标、最小/最大缩放和复位状态都有显式处理。

## 已完成：Memory 与本地 Repository

- 集中定义 `Memory`、`CreateMemoryInput`、`UpdateMemoryInput`、心情、分类和来源类型。
- Memory 包含：`id`、`text`、`imagePaths`、`placeName`、`mood`、`category`、`mapXRatio`、`mapYRatio`、`recordedAt`、`origin`、`createdAt`、`updatedAt`。
- 正文最多 2000 字、地点最多 60 字、照片最多 3 张；照片或非空正文至少存在一项。
- 日期统一保存为标准 ISO 字符串并按 `recordedAt` 倒序读取。
- 页面不直接散落调用 `wx.setStorageSync`；Storage 细节由 `LocalMemoryRepository` 封装。
- Storage key：`sdu-memory:memories:v1`，快照 `schemaVersion=1`。
- Repository 支持 list/get/create/update/delete、重复 ID 防护、非法 ID、记录不存在、读写失败、损坏 JSON 和未知 schema 错误。
- Demo Memory 使用 `origin=demo`，用户创建使用 `origin=user`。
- Demo 只在 Storage key 从未存在时 Seed；用户把数据删除到空数组后不会再次自动灌入。

## 已完成：照片持久化

- 使用 `wx.chooseMedia` 选择相册或相机图片，请求压缩结果，页面限制总数为 1—3 张并拒绝压缩后超过 5 MiB 的单张图片。
- 临时图片不会直接写入 Memory。
- 图片通过 FileSystemManager 保存到：

  ```text
  USER_DATA_PATH/sdu-memory/images/<memoryId>/
  ```

- 新建时先保存全部新图，再写 Repository；任一图片失败会回滚本轮已保存文件。
- Repository 写入失败时清理本轮新增图片，不创建引用无效路径的日记。
- 编辑时先保存新图，成功更新 Repository 后才清理被移除且不再被其他日记引用的旧图。
- 删除时先删除 Repository 数据，再查询剩余引用，只清理不再被引用的受管文件。
- 图片服务拒绝清理受管目录外路径和父目录穿越路径。
- 图片丢失会在地图、编辑或详情中降级并给出提示，不导致白屏。

## 已完成：页面闭环

- 新增“记录”Tab 启动页；真正的 editor 现在是普通页面，可以接收比例坐标或 `memoryId`。
- 从“记录”Tab 可以切到地图并自动进入一次性选点模式。
- editor 支持新建和编辑回显、照片添加/移除/预览、正文、地点、心情、分类、日期位置展示、保存和取消。
- editor 使用页面保存锁与 Repository 重复 ID 双重保护。
- 未保存返回使用 `wx.enableAlertBeforeUnload`，取消按钮另有确认。
- detail 展示全部照片、正文、日期、地点、心情、分类和比例位置。
- detail 支持编辑、删除二次确认、删除锁、部分图片清理失败提示和返回地图。
- 时间轴已支持本地月份列表、关键词搜索和本机个人资料；个人页仍保持基础内容。

## 自动检查结果

- 坐标纯函数：27 项通过。
- Memory、Repository、个人资料、搜索筛选与月份分组：39 项通过。
- 本地图片服务：5 项通过。
- 自动测试总计：71 项通过。
- TypeScript 严格类型检查通过。
- ESLint 检查通过。
- 完整 Prettier / `npm run check` 结果以本轮最终执行记录为准。

自动测试覆盖数据校验、CRUD、日期排序、非法/重复 ID、非法比例、空 Repository、损坏快照、写入失败不覆盖旧值、Demo 单次 Seed、缩放中心反算、图片部分失败回滚和受管目录清理边界。

## 尚未验证

- 当前完整闭环在微信开发者工具中的首次编译、WXML/WXSS 运行时和构建 npm。
- 两种模拟器尺寸下的中心准星比例校准、拖动、双指缩放和星点漂移。
- 相册取消、相册权限、相机、1—3 张真实照片复制和用户目录路径。
- 保存后重启、编辑换图、删除图片清理和损坏 Storage 的微信运行时表现。
- 至少一台真实手机上的触控、前后台切换、微信进程重开、持久化照片、性能和安全区。
- 替换地图后的真实主包大小、`miniprogram_npm` 大小、编译耗时和图片解码表现。
- 当前地图的生成过程、源文件、比赛使用权和正式素材决策。

不得把以上内容描述为已经通过真机测试。

## 已知限制

- 当前没有星点聚合；日记多时标记可能重叠。
- 星点会随地图一起缩放，尚未实现屏幕尺寸恒定的苹果照片地图式标记。
- 本地照片暂未做统一压缩、总容量统计或孤儿文件后台巡检。
- 清除小程序数据或卸载小程序会删除本地 Storage 和用户文件。
- 当前地图包含较大边界外留白；正式裁切或换图前必须迁移历史比例坐标。
- 不支持从编辑页重新选择位置；如需改变地点，可新建一条回忆。

## 已完成：时光搜索与地图显示筛选

- “时光”页增加本地关键词搜索，可同时匹配正文、地点、心情名称和内容主题名称；多个关键词按“全部命中”处理。
- 搜索结果继续按月份和记录时间排序；无匹配结果时显示可恢复的空状态，不会修改 Repository 数据。
- 地图右上角增加显示筛选面板，可选择近 1 天、近 7 天、近 30 天或自定义起止日期。
- 地图可继续按心情与内容主题组合筛选，界面显示“当前显示 / 可显示”的标记数量，并支持一键清除筛选。
- 地图默认标记改为小尺寸心情色点，不同心情使用不同颜色；用户可以在筛选面板中切换回照片标记。
- 筛选只影响地图和时光页面的显示结果，不删除、不覆盖已有本地回忆。
- 筛选和搜索复用 `utils/memory-filters.ts`，已覆盖关键词、自定义标签、日期边界、组合条件和输入不变性测试。

## 已修复：放大地图拖动回弹

- 移除手指松开后基于另一套缩放坐标执行的二次位置校正，避免与 `movable-view` 原生边界产生冲突。
- `movable-view` 允许短暂越界并由原生阻尼平滑收束，不再在地图内部出现提前阻挡的“空气墙”。
- 地图仍位于带视口级留白的有限画布中，不能无限拖走；中心准星最终坐标继续通过实际渲染矩形计算。

## 已修复：地图可停留在大片空白区域

- 普通浏览模式在拖动或惯性停止后执行轻量边界收束，地图最多只露出约 10 px 的边缘阻尼，不再允许带留白画布停成大面积空白。
- 拖动过程中仍由 `movable-view` 原生处理，不高频 `setData` 抢夺手势；约 140 ms 无新位移后才校正，因此不会在校园内部形成新的“空气墙”。
- 选点模式继续使用“地图中心必须位于画布内”的较宽边界，保留将四角移动到中央准星的能力；退出选点后恢复浏览边界。

## 已修复：地图筛选未移除标记

- 日期、心情和内容主题的筛选状态与过滤后的 `markers` 现在通过同一次 `setData` 原子更新，避免页面显示已选中、标记仍使用上一轮状态。
- 标记渲染 key 包含当前日期、心情、主题和显示模式；切换筛选时 WebView 会真正移除不匹配的旧标记节点。
- 筛选只改变地图显示，不删除 Repository 中的回忆；“清除筛选”会恢复全部与当前地图版本兼容的合法标记。

## 已完成：时光个人资料与校区称呼

- “时光”页顶部增加本机个人资料卡，可设置和修改名字、个性签名；名字最多 20 字，签名最多 80 字。
- 资料通过独立的 `LocalUserProfileRepository` 保存到 `sdu-memory:user-profile:v1`，不会修改或覆盖已有日记。
- 校区显示名称由地图配置统一提供，当前明确为“山东大学中心校区”；页面标题默认显示“我的中心校区时光”，设置名字后显示“名字的中心校区时光”。
- 资料仅保存在当前设备，不读取微信头像或昵称，不加入登录、多用户、公开主页或其他社交能力。
- 个人资料的空数据、规范化保存、字段长度、损坏 JSON 和写入失败已有 6 项纯逻辑测试；微信页面显示和重启持久化仍需人工验收。

## 已完成：本地数据公测版发布加固

- 工程版本号调整为 `0.9.0`，运行模式从 `demo` 调整为 `local`；正式页面不再自动写入三条演示日记。
- “时光”页增加“仅保存在这台设备”提示和数据管理入口；数据管理页说明无云备份、换机丢失和非 GPS 比例位置。
- 增加“清除全部本机数据”操作：二次确认后清空 Memory 快照、个人资料和受管照片目录，失败时显示部分完成状态。
- 添加照片前明确告知照片仅保存在当前设备，用户可以取消并继续纯文字记录。
- 地图源 PNG 已从 Git 候选范围排除；开发者工具上传包排除 Demo Seed、纯逻辑测试和目录说明文件。
- 当前仍是本地数据公测候选，不含登录、CloudBase、GPS、社交或跨设备备份。

## 下一步

先由人工验收 `AGENTS.md` 与 `docs/SOCIAL_V2_DESIGN.md` 中的第二阶段范围、可见性语义和安全门禁。验收通过后的唯一下一里程碑是“CloudBase 环境与可信身份基础”：只建立安全环境配置、用户集合和服务端身份接口，并完成单账号与双账号越权测试框架。

在本设计里程碑通过前，不创建云资源，不实现好友、点赞、云端回忆或 24 小时好友地图，也不自动迁移现有本地数据。
